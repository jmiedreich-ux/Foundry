# Overlay and navigation controls

## Outcome

Dialog, Drawer, Popover, Menu, Tabs, and Card provide their approved focus, dismissal, selection, and semantic behavior through the public library and gallery.

## Remaining work

Menu, Tabs, and Card browser checks remain. Menu browser checks are paused by owner. Base UI 1.8.0 is selected; before work resumes, the library quality foundation must approve its production integration contract and the remaining token, skin, package, and verification contracts. Existing observable behavior is evidence; the current hand-built internals are not presumed to be retained.

## Dependencies

The library quality foundation is complete, including the reviewed Base UI production integration contract and its required verification gates.

## Completion evidence

Every control has real gallery behavior, focused browser coverage, and independent review. Modal controls contain and restore focus; non-modal controls preserve the documented dismissal and focus behavior.

# M4 — Overlay and navigation controls

## Ordered packets

| Packet | Owner role | Owned paths | Depends on | Outcome |
| --- | --- | --- | --- | --- |
| CG-M4-01 | overlays | `packages/react/src/overlays/**` | M1 | Overlay root, Dialog, Drawer, Popover, and Menu. |
| CG-M4-02 | navigation | `packages/react/src/navigation/**`, `packages/react/src/layout/**` | M1 | Tabs and Card. |
| CG-M4-03 | gallery | `apps/lab/src/examples/overlays/**`, `apps/lab/src/examples/navigation/**` | M4-01, M4-02 | Working layer and navigation demonstrations. |
| CG-M4-04 | specifications | `tests/overlays/**`, `tests/navigation/**` | M4-03 | Focus, Escape, dismissal, and keyboard coverage. |

## Acceptance criteria

- Dialog and Drawer trap/contain focus as specified, close on Escape, and restore focus to their trigger.
- Popover and Menu use their documented outside-dismissal behavior and do not leave orphaned open state.
- Tabs support pointer selection and Arrow-key movement, with one selected tab and one visible panel.
- All overlay and navigation examples work at narrow and wide widths.

## Invariants

- No hidden overlay retains focus.
- Closing an overlay restores focus to its last valid trigger.
- Exactly one tab panel is visible for each tab set.

# M3 — Actions, choices, search, and feedback

## Ordered packets

| Packet | Owner role | Owned paths | Depends on | Outcome |
| --- | --- | --- | --- | --- |
| CG-M3-01 | actions | `packages/react/src/actions/**` | M1 | Button variants, category/label resolution, domain-state override, and disabled/loading contract. |
| CG-M3-02 | choices | `packages/react/src/inputs/choice/**`, `packages/react/src/inputs/search/**` | M2 | Checkbox, RadioGroup, Switch, and Search. |
| CG-M3-03 | feedback | `packages/react/src/feedback/**` | M1 | StatusChip, Banner, Toast, EmptyState, and LoadingSkeleton. |
| CG-M3-04 | gallery | `apps/lab/src/examples/actions/**`, `apps/lab/src/examples/choices/**`, `apps/lab/src/examples/feedback/**` | M3-01, M3-02, M3-03 | Real behavior examples and cross-control composition. |
| CG-M3-05 | specifications | `tests/actions/**`, `tests/choices/**`, `tests/feedback/**` | M3-04 | Browser and focused behavior coverage. |

## Acceptance criteria

- Button variants are visually distinct and invoke their expected gallery behavior.
- Disabled/loading buttons cannot submit or duplicate an action.
- Checkboxes, switches, and radio groups work with pointer and keyboard input and reset correctly.
- Search has empty, matching, and no-match states.
- Toasts announce, banners can be dismissed/restored, and loading/empty states are observable.

## Invariants

- A radio group has one selected value when it is required.
- Feedback text gives an outcome or recovery, not only a color signal.
- Repeated activation cannot create an unbounded duplicate action.
